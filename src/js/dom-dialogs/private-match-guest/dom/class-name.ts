/** ルートHTML要素のclass属性 */
export const ROOT_CLASS = "private-match-guest";

/** エントリーフォーム */
export const ENTRY_FORM = `${ROOT_CLASS}__entry-form`;

/** エントリーフォーム非表示 */
export const ENTRY_FORM_INVISIBLE = `${ENTRY_FORM}--invisible`;

/** マッチング中 */
export const MATCHING = `${ROOT_CLASS}__matching`;

/** マッチング中非表示 */
export const MATCHING_INVISIBLE = `${MATCHING}--invisible`;
