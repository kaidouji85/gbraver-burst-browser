/** ルートHTML要素のclass属性 */
export const ROOT_CLASS = "message-window";

/** ルートHTML要素が非表示の際のclass属性 */
export const ROOT_CLASS_INVISIBLE = `${ROOT_CLASS}--invisible`;

/** ルート要素が左側表示されている時のclass属性 */
export const ROOT_CLASS_LEFT = `${ROOT_CLASS}--left`;

/** ルート要素が右側表示されている時のclass属性 */
export const ROOT_CLASS_RIGHT = `${ROOT_CLASS}--right`;

/** ルート要素がバッテリーセレクタの隣に表示されている時のclass属性 */
export const ROOT_CLASS_NEAR_BATTERY_SELECTOR = `${ROOT_CLASS}--near-battery-selector`;

/** ルート要素がバーストボタンの隣に表示されている時のclass属性 */
export const ROOT_CLASS_NEAR_BURST_BUTTON = `${ROOT_CLASS}--near-burst-button`;

/** ルート要素がパイロットボタンの隣に表示されている時のclass属性 */
export const ROOT_CLASS_NEAR_PILOT_BUTTON = `${ROOT_CLASS}--near-pilot-button`;

/** ルート要素がプレイヤー側叫びウインドウの場合のclass属性 */
export const ROOT_CLASS_PLAYER_SHOUT = `${ROOT_CLASS}--player-shout`;

/** ルート要素が敵側叫びウインドウの場合のclass属性 */
export const ROOT_CLASS_ENEMY_SHOUT = `${ROOT_CLASS}--enemy-shout`;

/** 次メッセージアイコンのclass属性 */
export const NEXT_MESSAGE_ICON_CLASS = `${ROOT_CLASS}__next-message-icon`;

/** 次メッセージアイコン非表示時のclass属性 */
export const NEXT_MESSAGE_ICON_CLASS_INVISIBLE = `${NEXT_MESSAGE_ICON_CLASS}--invisible`;
