/** ルートHTML要素 class属性 */
export const ROOT = "mini-controller";

/** バッテリーボタン class属性 */
export const BATTERY = `${ROOT}__battery`;

/** バッテリーボタン 先頭 class属性 */
export const BATTERY_FIRST = `${BATTERY}--first`;

/** バッテリーボタン 末尾 class属性 */
export const BATTERY_LAST = `${BATTERY}--last`;

/** バッテリーボタン 非表示 class属性 */
export const BATTERY_INVISIBLE = `${BATTERY}--invisible`;

/** バーストボタン class属性 */
export const BURST = `${ROOT}__burst`;
